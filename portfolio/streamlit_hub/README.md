# Portfolio Streamlit hub (local)

Unified multipage Streamlit app linking all four Career DS OS projects.

```bash
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
bash scripts/run_check.sh
streamlit run Home.py
```

**Do not** push to GitHub or deploy Streamlit Cloud without user confirmation.
